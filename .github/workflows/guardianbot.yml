# Generated by guardianctl. Scanner logic remains in Geekyshubham/guardianbot.
name: GuardianBot

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
  push:
    branches: ["main"]
  schedule:
    - cron: "23 2 * * *"
  workflow_dispatch:

permissions:
  contents: read
  security-events: write
  actions: read

jobs:
  guardianbot-security-gate:
    name: guardianbot/security-gate
    uses: Geekyshubham/guardianbot/.github/workflows/reusable-security.yml@441ca4877d0a934f2a076971e4b2a6763093be67
    with:
      config-path: .guardianbot/config.yml
