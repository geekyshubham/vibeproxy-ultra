# Generated by guardianctl. Scanner logic remains in Geekyshubham/guardianbot.
name: GuardianBot

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
  push:
    branches: ["main"]
  schedule:
    - cron: "23 2 * * *"
  workflow_dispatch:

permissions:
  contents: read
  security-events: write
  actions: read

jobs:
  guardianbot-security-gate:
    name: guardianbot/security-gate
    uses: Geekyshubham/guardianbot/.github/workflows/reusable-security.yml@a1a5640b4aa30bf393777e420b3c9049739af7e7
    with:
      config-path: .guardianbot/config.yml
