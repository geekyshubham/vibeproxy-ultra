# Generated by guardianctl. Scanner logic remains in Geekyshubham/guardianbot.
name: GuardianBot

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
  push:
    branches: ["main"]
  schedule:
    - cron: "23 2 * * *"
  workflow_dispatch:

permissions:
  contents: read
  security-events: write
  actions: read

jobs:
  guardianbot-security-gate:
    name: guardianbot/security-gate
    uses: Geekyshubham/guardianbot/.github/workflows/reusable-security.yml@441ca48d5ca1574826c7738bd3c18bc50a60e58b
    with:
      config-path: .guardianbot/config.yml
