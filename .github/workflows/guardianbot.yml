# Generated by guardianctl. Scanner logic remains in Geekyshubham/guardianbot.
name: GuardianBot

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
  push:
    branches: ["main"]
  schedule:
    - cron: "23 2 * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  guardianbot-security-gate:
    name: guardianbot/security-gate
    if: github.event_name != 'schedule' || github.event.schedule == '23 2 * * *'
    permissions:
      contents: read
      security-events: write
      actions: read
      id-token: write
    uses: Geekyshubham/guardianbot/.github/workflows/reusable-security.yml@152649be5a86862f619a86d60598fc25bafb0429
    with:
      config-path: .guardianbot/config.yml


